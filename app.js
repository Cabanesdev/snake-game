$(document).ready(function () {
	//Canvas configuration
	let canvas = $('#canvas')[0];
	const ctx = canvas.getContext('2d');
	let width = canvas.width();
	let height = canvas.height();

	let cellWidth = 15;
	let direction, food, score, level;
	let snake_length;
});
